# AI-Powered Professional Speech Correction Assistant
# Python 3.10+

# Speech-to-text
openai-whisper>=20231117

# Audio recording (microphone input)
sounddevice>=0.4.6
numpy>=1.24.0

# UI
streamlit>=1.32.0

# Audio format support (MP3, M4A decoding for Whisper)
ffmpeg-python>=0.2.0

# ── NEW: Webcam posture analysis ──────────────────────────────────────────
mediapipe>=0.10.9
opencv-python-headless>=4.9.0   # headless = no GUI window needed (Streamlit handles display)

# Utilities (already stdlib, listed for clarity)
# pathlib, json, wave, tempfile, argparse, urllib, sqlite3 — all built-in